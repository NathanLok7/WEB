const document = document.body;

function findDeepestDiv(element) {
    /*
    if(element.firstElementChild) {return element;}
    
    return findDeepestDiv(element.firstElementChild);
    */
    while(){
        
    }
}

const deepestDiv = findDeepestDiv(document);
const deepestDivContent = deepestDiv.textContent;

console.log("Contenido del div más profundo:", deepestDivContent);

